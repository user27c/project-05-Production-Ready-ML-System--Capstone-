---
alwaysApply: true
description: "默认激活规则"
---

### Cursor AI 编码行为准则

## 1. 编码前先思考

**不要想当然。不要掩饰困惑。明确权衡取舍。**

在实现之前：
- 明确写出你的假设；如果不确定，就提问。
- 如果存在多种解释，全部说明，不要默默自行选择。
- 如果有更简单的方法，直接说出来；必要时应当提出异议。
- 如果有不清楚的地方，先停下来；指出困惑点并提问。

## 2. 简洁优先

**用最少代码解决问题。不要做任何猜测性扩展。**

- 不要添加超出需求范围的功能。
- 不要为一次性代码做抽象。
- 不要加入未被要求的“灵活性”或“可配置性”。
- 不要为不可能发生的场景添加错误处理。
- 如果你写了 200 行但其实 50 行就够，重写它。

问自己：“资深工程师会认为这过度复杂吗？”如果会，就简化。

## 3. 手术式修改

**只改必须改的部分。只清理你自己引入的问题。**

在编辑现有代码时：
- 不要“顺手优化”相邻代码、注释或格式。
- 不要重构没有坏掉的东西。
- 遵循现有风格，即使你个人有不同偏好。
- 如果发现无关的死代码，可以指出，但不要删除。

当你的改动产生“孤儿”代码时：
- 删除因你的改动而变成未使用的 import / 变量 / 函数。
- 不要删除预先存在的死代码，除非被明确要求。

检验标准：每一行变更都应能直接追溯到用户请求。

## 4. 目标驱动执行

**先定义成功标准，再循环执行直到验证通过。**

把任务转成可验证目标：
- “添加校验” → “先为非法输入写测试，再让测试通过”
- “修复 bug” → “先写能复现问题的测试，再让测试通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，给出简要计划：
1.[步骤] → 验证：[检查项]
2.[步骤] → 验证：[检查项]
3.[步骤] → 验证：[检查项]

强成功标准可以让你独立迭代；弱标准（如“让它能跑”）会导致反复澄清。

## 5. 其他规范

** 1. 提交git时, 使用中文总结此次提交内容**